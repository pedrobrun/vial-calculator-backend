version: '3.1'

services:
  mongo:
    image: mongo
    restart: always
    container_name: mongo_dashflow
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: 123456
    ports:
      - 12345:27017
